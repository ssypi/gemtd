Diamond = {}

function Diamond:OnCreated(keys)
    print("Diamond created")
    --TODO: fix not getting called
    --TODO: add crit ability, level up
end

function Diamond:OnAttackLanded(keys)
end

function Diamond:OnAttackStart(keys)
end